[package]
name = "extended-vft-client"
version = "0.1.0"
edition = "2021"

[dependencies]
sails-rs.workspace = true

[build-dependencies]
extended-vft-app = { path = "../app", default-features = false }
sails-rs = { workspace = true, features = ["build"] }

[features]
default = []
stress-tests = ["extended-vft-app/stress-tests"]
